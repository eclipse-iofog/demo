services:
  test-runner:
    container_name: test-runner
    environment:
    - LOCAL=1
    image: iofog/test-runner-develop:latest
    networks:
    - iofog
    volumes:
    - ./conf:/conf
version: '3'
volumes:
  conf: null
networks:
  iofog: null